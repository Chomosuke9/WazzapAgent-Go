import { StatusBadge } from "../components/StatusBadge";
export function AppDataPage() { return <div className="page"><header className="page-header"><div><p className="eyebrow">APP & DATA</p><h1>Your data stays yours.</h1><p className="lede">The local settings database is active. Backup, restore, and moving the data root are waiting for the data controller.</p></div><StatusBadge tone="good">Settings active</StatusBadge></header><section className="card data-card"><div className="data-icon">▣</div><div><h2>Settings persistence is active</h2><p className="muted">The app opens the settings database after acquiring the data root lease. Form changes can be read again after a restart.</p></div></section><section className="card capability-list"><div><strong>Available now</strong><span>Persistent settings and local bootstrap pointer</span></div><div><strong>Coming next</strong><span>Offline backup and restore with verification</span></div><div><strong>Not supported</strong><span>Moving the data root or deleting all data from the UI</span></div></section></div>; }
