# Entrypoint web berikutnya

Tempat bootstrap HTTP API jika mode browser mulai diimplementasikan.
Adapter HTTP nantinya memanggil use case yang sama dengan adapter Wails.
Autentikasi dan otorisasi akses remote harus ditentukan sebelum mode ini dibuka.

Status: hanya reservasi folder untuk tahap web; belum ada server baru.
CLI/headless yang sekarang tetap berada di `cmd/wazzapagent`.
